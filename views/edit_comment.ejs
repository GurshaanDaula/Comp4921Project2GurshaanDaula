<!DOCTYPE html>
<html lang="en">
<%- include('templates/header', { loggedIn, username, title: 'Home' }) %>

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit Comment</title>
        <link rel="stylesheet" href="/css/style.css" />
    </head>

    <body>

        <main class="container">
            <div class="glass-card">
                <h1 class="shiny-text">Edit Comment</h1>

                <form action="/comments/<%= comment.comment_id %>/edit" method="POST">
                    <textarea name="content" rows="5" required><%= comment.content %></textarea>
                    <div style="margin-top: 1rem;">
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                        <a href="/threads/<%= comment.thread_id %>" class="btn btn-outline">Cancel</a>
                    </div>
                </form>
            </div>
        </main>

        <footer>
            <p>© 2025 My Forum App. All rights reserved.</p>
        </footer>
    </body>

</html>