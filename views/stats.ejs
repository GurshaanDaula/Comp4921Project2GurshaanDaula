<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Stats - Most Popular Threads</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="navbar-brand shiny-text">My Forum</a>
            <div class="nav-links">
                <a href="/" class="nav-link">Home</a>
                <a href="/stats" class="nav-link active">Stats</a>
                <a href="/threads/new" class="nav-link">New Thread</a>
                <% if (loggedIn) { %>
                    <span class="nav-link">Logged in as <strong>
                            <%= username %>
                        </strong></span>
                    <a href="/logout" class="nav-link">Logout</a>
                    <% } else { %>
                        <a href="/login" class="nav-link">Login</a>
                        <% } %>
            </div>
        </div>
    </nav>

    <main class="container">
        <h1 class="shiny-text">Top Threads</h1>

        <% if (threads && threads.length) { %>
            <% threads.forEach(t=> { %>
                <div class="glass-card">
                    <div class="thread-header">
                        <h2 class="thread-title">
                            <a href="/threads/<%= t.thread_id %>">
                                <%= t.title %>
                            </a>
                        </h2>
                        <small>By <%= t.username %> • <%= new Date(t.created_at).toLocaleString() %></small>
                    </div>
                    <div class="thread-meta">
                        <span><i class="fas fa-heart"></i>
                            <%= t.total_likes %> Total Likes
                        </span>
                        <span><i class="fas fa-comment"></i>
                            <%= t.comment_count %> Comments
                        </span>
                        <span><i class="fas fa-eye"></i>
                            <%= t.views %> Views
                        </span>
                    </div>
                </div>
                <% }) %>
                    <% } else { %>
                        <p>No data to show right now.</p>
                        <% } %>
    </main>

    <footer>
        <p>© 2025 My Forum App. All rights reserved.</p>
    </footer>

</body>

</html>